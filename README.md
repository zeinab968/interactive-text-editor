# interactive-text-editor
This project is a basic text editor built using HTML, CSS, and Vanilla JavaScript. It demonstrates how to manipulate the DOM dynamically and update styles in real time based on user input.
